# Downloaded datasets, never committed. The leading slash matters: it confines the
# rule to the top-level folder, so it can't swallow the src/.../data package.
/data/

.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/
